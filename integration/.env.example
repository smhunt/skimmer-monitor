# MQTT broker (Mosquitto shared with sump pump monitor)
MQTT_URL=mqtt://192.168.1.10:1883

# Postgres (same instance as sump pump monitor ingestion)
DATABASE_URL=postgresql://localhost/iot

# Particle Cloud — needed by the MCP server's force_fill tool only
PARTICLE_TOKEN=
PARTICLE_DEVICE=skimmer-monitor
